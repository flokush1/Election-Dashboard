# Setup

Frontend: `npm install` then `npm run dev` (http://localhost:3000)

Backend: `pip install -r requirements.txt` then `python model_api.py` (http://127.0.0.1:5000)

Copy `.env.example` to `.env` and point `PREDICTIONS_DIR` / `EXCEL_DATA_PATH` at private data.

Tests: `npm test` and `pytest`
